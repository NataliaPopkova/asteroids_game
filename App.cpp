#include <iostream>
#include <vector>
#include <stdio.h>
#include <math.h>
#include <cstdlib>

#include "Engine.h"

using namespace std;

int main(int argc, char** argv)
{

    // srand(static_cast<unsigned int>(time(0)));
    draw();
    
    return 0;
}